# IMDB-clone-Main
Hey there,Built with Next.js and React, this SSR-safe theming system supports dark, high-contrast, and auto modes using CSS variables. Preferences persist via cookies, LocalStorage, and DB (Prisma). It uses Framer Motion for smooth transitions, CSP nonce security, per-route overrides, and ensures zero CLS verified by Lighthouse.
